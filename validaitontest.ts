function validation(message:string) {
    const valid = message
     switch (valid){
     case valid.match(/\w+@\w+\.\w+/gm) : // mail
     console.log('eto mail') ;
    }
}
validation('url@mail.ru')
